# micro:bit 게임 컨트롤러 수업 설계

## 개요

학생들이 micro:bit를 게임 컨트롤러로 사용하되, **컨트롤러 코드를 직접 작성**하여 게임 성적을 높이는 코딩 수업. 게임은 교사가 웹앱으로 제공하고, 학생은 micro:bit 코드만 작성한다.

## 시스템 구조

```
┌─────────────────┐        USB Serial        ┌──────────────────────┐
│   micro:bit     │ ── "jump\n"  ─────────→ │   브라우저 (게임)      │
│   (학생이 코딩)   │ ── "slide\n" ─────────→ │   WebSerial API       │
│                 │ ── "double\n" ────────→ │   Canvas 2D 게임      │
└─────────────────┘                          └──────────────────────┘
     MicroPython / MakeCode                    React + Node.js 서버
```

- **게임 서버**: Express + SQLite. 게임 페이지 서빙 + 리더보드 API
- **게임**: 시리얼로 들어오는 명령어 문자열을 게임 조작으로 처리
- **micro:bit**: 버튼/터치 입력을 읽어 명령어 문자열을 시리얼로 전송
- **키보드 입력 차단**: `?keyboard=true` 없으면 micro:bit 시리얼만 유일한 입력 경로

## micro:bit 4가지 입력

| # | 입력 | 코드 (Python) | 코드 (블록) |
|---|------|---------------|-------------|
| 1 | A 버튼 | `button_a.was_pressed()` | 버튼 A 눌림 |
| 2 | B 버튼 | `button_b.was_pressed()` | 버튼 B 눌림 |
| 3 | 로고 터치 (V2) | `pin_logo.is_touched()` | 로고 터치됨 |
| 4 | A+B 동시 | `button_a.is_pressed() and button_b.is_pressed()` | (블록 조합 필요) |

> A+B 동시 입력은 A단독/B단독과 구분하는 로직이 필요하여 ★★★ 난이도.

---

## 핵심 컨셉: 피지컬 vs 코딩 — 두 가지 길로 1등하기

### 기본 제공 코드

모든 학생에게 동일한 기본 컨트롤러 코드를 제공한다. **명령어 1개만 매핑**되어 있다.

**Python:**
```python
from microbit import *

while True:
    if button_a.was_pressed():
        print("jump")
    sleep(30)
```

**블록 (MakeCode JavaScript):**
```javascript
basic.forever(function () {
    if (input.buttonIsPressed(Button.A)) {
        serial.writeLine("jump")
    }
    basic.pause(30)
})
```

**의도적 제약:**
- 명령어 1개만 전송 (나머지 기능은 잠겨 있음)
- 나머지 3개 입력(B, 로고, A+B)은 비어 있음 → 코딩으로 채워야 함

### 경로 1: 피지컬로 1등하기

기본 코드 그대로 사용하되 신체 능력으로 극복한다.

- 1개 명령어로 최적 타이밍 플레이
- 패턴 암기와 예측으로 부족한 조작 극복 (시드 고정으로 매판 같은 패턴)
- 게임 감각이 좋은 학생에게 유리

### 경로 2: 코딩으로 1등하기

micro:bit 코드를 수정하여 컨트롤러 자체를 업그레이드한다.

| 레벨 | 개선 내용 | 효과 |
|---|---|---|
| ★☆☆ | B버튼에 2번째 명령어 매핑 | 새로운 액션 해금 |
| ★★☆ | 로고 터치에 3번째 명령어 매핑 | 풀 플레이 가능 |
| ★★★ | A+B 동시 입력으로 4번째 명령어 매핑 | 특수 기능 해금 |

### 밸런스

```
피지컬 장인 + 기본코드  →  높은 점수 가능 (노력형)
코딩 고수 + 풀업코드    →  높은 점수 가능 (효율형)
피지컬 + 코딩 둘 다     →  최고 점수 (완성형)
```

코딩은 자동 플레이가 아니라 **조작 해금**이다. 잠긴 기능을 열어주는 것이지 게임을 대신 해주는 것이 아니므로, 결국 게임 실력도 필요하다.

---

## 게임 목록

> 모든 게임의 명령어는 영단어. micro:bit에서 `print("명령어")`로 전송.
> 장애물/적 패턴은 시드 고정 → 매판 동일 → 공정한 경쟁.
> 리더보드는 1인 1건 최고점만 유지, 게임오버 시 자동 등록.

### 1. 러너 (Dino Run 스타일) — 구현 완료

**장르:** 액션/러너

**명령어 (3개 사용, 최대 4개 확장 가능):**

| 명령어 | 동작 | 기본 매핑 |
|--------|------|-----------|
| `jump` | 점프 | A 버튼 (기본 제공) |
| `slide` | 슬라이드 | ★☆☆ 해금 |
| `double` | 더블점프 | ★★☆ 해금 |

**기본코드로 가능한 것:** 점프만 (높은 장애물은 타이밍 맞추면 넘김)

**장애물 종류:**
- **높은 블록**: 점프로 넘기 (0초~)
- **콤보 벽**: 높아서 더블점프 필요, 타이밍 완벽하면 일반 점프로도 가능 (3초~)
- **게이트**: 위아래 막힌 통로, 슬라이드로만 통과 (10초~)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 슬라이드 | B버튼 → `slide` | 게이트 통과 가능 |
| ★★☆ | 더블점프 | 로고 터치 → `double` | 콤보 벽 쉽게 넘기 |

**점수:** 생존 거리 (시간 × 10). 속도는 시간이 지날수록 증가.

---

### 2. 테트리스

**장르:** 퍼즐

**명령어 (4개):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `left` | 왼쪽 이동 | 기본 제공 |
| `right` | 오른쪽 이동 | ★☆☆ |
| `rotate` | 회전 | ★★☆ |
| `drop` | 하드드롭 | ★★★ |

**기본코드로 가능한 것:** 왼쪽 이동만 (오른쪽도 없음)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 오른쪽 이동 | B버튼 → `right` | 좌우 이동 가능 |
| ★★☆ | 회전 | 로고 터치 → `rotate` | 블록 배치 자유도 확보 |
| ★★★ | 하드드롭 | A+B 동시 → `drop` | 속도 혁신 |

**점수:** 라인 클리어 + 콤보 보너스 + 테트리스(4줄) 보너스

---

### 3. 비행 슈팅 (탄막 슈팅)

**장르:** 슈팅

**명령어 (4개):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `left` | 왼쪽 | 기본 제공 |
| `right` | 오른쪽 | ★☆☆ |
| `fire` | 발사 | ★★☆ |
| `bomb` | 폭탄 (게임당 3회) | ★★★ |

**기본코드로 가능한 것:** 왼쪽 이동만 (총도 안 나감, 피하기만)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 오른쪽 | B버튼 → `right` | 좌우 회피 가능 |
| ★★☆ | 발사 | 로고 터치 → `fire` | 드디어 총이 나감 |
| ★★★ | 폭탄 | A+B 동시 → `bomb` | 위기 탈출 |

**밸런스:**
- 생존 시간만으로도 점수 → 피지컬 보상
- 적 처치 시 추가 점수 → 코딩 보상
- 폭탄 게임당 3회 제한

**점수:** 생존 시간 + 적 처치 보너스

---

### 4. 리듬 게임

**장르:** 음악/리듬

**명령어 (4개 = 4레인):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `hit1` | 레인 1 타격 | 기본 제공 |
| `hit2` | 레인 2 타격 | ★☆☆ |
| `hit3` | 레인 3 타격 | ★★☆ |
| `hit4` | 레인 4 타격 | ★★★ |

**기본코드로 가능한 것:** 1레인만 판정

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 2레인 | B버튼 → `hit2` | 판정 가능 레인 2배 |
| ★★☆ | 3레인 | 로고 터치 → `hit3` | 대부분의 노트 대응 |
| ★★★ | 4레인 | A+B 동시 → `hit4` | 풀 플레이 가능 |

**점수:** Perfect/Great/Good/Miss 판정 + 콤보 배율

---

### 5. 벽돌깨기 (Breakout)

**장르:** 아케이드

**명령어 (4개):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `left` | 패들 왼쪽 | 기본 제공 |
| `right` | 패들 오른쪽 | ★☆☆ |
| `launch` | 공 발사 | ★★☆ |
| `skill` | 스킬 (공 분열) | ★★★ |

**기본코드로 가능한 것:** 왼쪽 이동만 (공은 자동 발사, 스킬 없음)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 오른쪽 | B버튼 → `right` | 좌우 패들 이동 |
| ★★☆ | 공 발사 | 로고 터치 → `launch` | 발사 타이밍 조절 |
| ★★★ | 스킬 | A+B 동시 → `skill` | 공 분열로 빠른 클리어 |

---

### 6. 탱크 배틀

**장르:** 전략/액션

**명령어 (4개):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `forward` | 전진 | 기본 제공 |
| `turnl` | 좌회전 | ★☆☆ |
| `turnr` | 우회전 | ★★☆ |
| `fire` | 발사 | ★★★ |

**기본코드로 가능한 것:** 전진만 (회전 불가, 발사 불가 → 도망만)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 좌회전 | B버튼 → `turnl` | 방향 전환 시작 |
| ★★☆ | 우회전 | 로고 터치 → `turnr` | 자유로운 이동 |
| ★★★ | 발사 | A+B 동시 → `fire` | 공격 가능 |

---

### 7. 뱀서라이크 (Vampire Survivors 스타일)

**장르:** 서바이벌 액션

**명령어 (4개):**

| 명령어 | 동작 | 해금 |
|--------|------|------|
| `left` | 왼쪽 | 기본 제공 |
| `right` | 오른쪽 | ★☆☆ |
| `up` | 위 | ★★☆ |
| `down` | 아래 | ★★★ |

**기본코드로 가능한 것:** 왼쪽만 (공격은 자동이므로 생존은 가능)

**코딩 해금 순서:**

| 레벨 | 해금 | 매핑 예시 | 효과 |
|---|---|---|---|
| ★☆☆ | 오른쪽 | B버튼 → `right` | 좌우 이동 |
| ★★☆ | 위 | 로고 터치 → `up` | 상하좌우 이동 |
| ★★★ | 아래 | A+B 동시 → `down` | 완전한 4방향 |

---

## 수업 운영 계획

### 차시별 진행

| 차시 | 게임 | 학습 목표 | 코딩 포인트 |
|---|---|---|---|
| 1차시 | 러너 | micro:bit 연결, 기본 코드 이해 | 명령어 1~2개 추가 |
| 2차시 | 벽돌깨기 | 블록코딩 vs Python 비교 | 명령어 3개 매핑 |
| 3차시 | 테트리스 | 4개 명령어 완성 | A+B 동시 입력 도전 |
| 4차시 | 비행 슈팅 | 전략적 명령어 배치 | 어떤 입력에 어떤 명령어? |
| 5차시 | 자유 선택 | 전 게임 대상 코드 최적화 | 자유 코딩 |
| 토너먼트 | 전 게임 | 리더보드 종합 순위 | 최종 경쟁 |

### 동기부여 설계

**1. 리더보드**
- 게임별 실시간 순위 표시 (관리자 페이지)
- 1인 1건 최고점만 유지
- 교실 TV/프로젝터에 상시 표시
- 시드 고정 → 같은 패턴에서 공정 경쟁

**2. 단계적 해금 체감**
- 1차시: 명령어 1개 → "아 이것밖에 안 돼?"
- 2차시: 명령어 2개 → "오 이게 되네!"
- 3차시: 명령어 3개 → "거의 다 할 수 있다"
- 4차시: 명령어 4개 → "풀 컨트롤!"

**3. 단계적 힌트 제공**
- 1차시: 게임 명령어 목록만 공개
- 2차시: "B버튼으로 뭔가 할 수 있을 것 같은데..."
- 3차시: "로고를 만져봤어?"
- 4차시: "A랑 B를 동시에 누르면?"
- 직접 알려주지 않고 탐구하게 유도

**4. 코드 공유 금지 → 발표로 대체**
- 서로 코드를 복사하지 못하게 하되
- 토너먼트 후 상위 학생이 "내 코드의 핵심" 발표
- 코딩 실력이 곧 전략적 우위라는 인식 형성

### 기술 스택

| 구성요소 | 기술 |
|---|---|
| 서버 | Node.js + Express + SQLite (단일 exe 배포 가능) |
| 게임 렌더링 | HTML5 Canvas 2D |
| 시리얼 통신 | WebSerial API (Chrome 89+) |
| 컨트롤러 | micro:bit V2 + MicroPython 또는 MakeCode 블록코딩 |
| 리더보드 | SQLite (1인 1건 최고점), REST API 폴링 |
| 프론트엔드 | React + Vite |

### 통신 프로토콜

게임은 시리얼로 들어오는 **줄바꿈(`\n`) 구분 문자열**을 명령어로 인식한다.

```
명령어\n
```

- 게임별 인식 명령어가 다름 (영단어)
- 인식하지 못하는 문자열은 무시
- Python: `print("jump")` / 블록: `serial write line "jump"`
- 명령어 쿨다운으로 자동화 방지

### 게임 내 자동화 방지

| 방지 수단 | 설명 |
|---|---|
| 명령어 쿨다운 | 같은 명령어가 너무 빠르게 들어오면 무시 |
| 리소스 제한 | 폭탄, 스킬 등은 게임당 횟수 제한 |
| 시드 고정 패턴 | 암기/연습이 가능하지만 자동화는 아님 |
| 사람이 판단해야 하는 요소 | 테트리스 블록 배치, 슈팅 회피 등은 자동화 불가 |
| 키보드 차단 | `?keyboard=true` 없으면 micro:bit만 입력 가능 |
